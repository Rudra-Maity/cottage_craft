<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shopping Cart</title>
    <link rel="stylesheet" href="Cart Page.css">
    <script src="Cart Page.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 10px 0;
        }

        main {
            display: flex;
            justify-content: space-between;
            padding: 20px;
        }

        .products,
        .cart {
            width: 48%;
            border: 1px solid #ccc;
            padding: 10px;
        }

        .product,
        .cart-item {
            border: 1px solid #ddd;
            padding: 10px;
            margin: 10px 0;
            width:100%;
        }

        button.add-to-cart {
            background-color: #4caf50;
            color: #fff;
            border: none;
            padding: 5px 10px;
            cursor: pointer;
        }

        button.add-to-cart:hover {
            background-color: #45a049;
        }

        main {
            display: flex;
            justify-content: center;
        }

        * {
            font-size: 110%;
        }
        a{
            text-decoration: none;
        }
    </style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
</head>

<body>
    <header>
        <h1>Shopping Cart</h1>
    </header>
    <main>
        <%if(doc.length>0){%>
        <div class="product">
            <%doc.forEach(item => {%>
                <img class="mt-4" src="data:<%= item.images[0].contentType %>;base64, <%= item.images[0].data.toString('base64') %>" alt="<%= item.images[0].imgname %>" >
                <a href="/api/v1/product/<%=item.pid%>" target="_blank">
                <p class="card-text"><%=item.name%></p></a>
                    <p>&#8377; <%=item.price%></p>
                    <a href="/api/v1/user/orderCheckOut?pid=<%=item.pid%>" class="add-to-cart btn btn-success">Place Order</a>

                    <a href="/api/v1/user/cart/remove?pid=<%=item.pid%>" class="add-to-cart btn btn-success">remove cart</a>
                    
                    <br>
                    <%})%>

        </div>
        <%}else{%>
            <div class="card">
                <div class="card-header">
                  <p class="card-text">You have not any cart items go to for shopping</p>
                  <a href="/" class="btn btn-primary">Go</a>
                </div>
              </div>
            <%}%>
    </main>
</body>

</html>